server:
  port: ${EUREKA_SERVER_PORT:8761}

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    enableSelfPreservation: false

---
spring:
  profiles: docker
  application:
    name: eureka-server
server:
  port: ${EUREKA_SERVER_PORT:8761}

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
